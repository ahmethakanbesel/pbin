---
phase: 03-buckets-and-paste
plan: "02"
subsystem: domain
tags: [bucket, sqlite, zip, bcrypt, filestore, repository-pattern]

# Dependency graph
requires:
  - phase: 03-01
    provides: migrations adding delete_secret column to buckets table
  - phase: 02-file-sharing
    provides: file.Service and fileRepo patterns to replicate
provides:
  - BucketService with Create/GetMeta/StreamZIP/Delete business logic
  - bucketRepo SQLite implementation of bucket.Repository
  - Extended Bucket entity with DeleteSecret, ExpiresAt fields and sentinel errors
affects:
  - 03-04 (bucket handler depends on BucketService and bucketRepo)
  - 03-05 (cleanup/expiry worker depends on bucket.Repository.ListExpired)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Filestore-first upload: write bytes before DB insert, best-effort store.Delete rollback on DB failure
    - Atomic one-use enforcement: UPDATE WHERE downloaded_at IS NULL + RowsAffected (prevents TOCTOU race)
    - Timing-safe delete secret check via subtle.ConstantTimeCompare
    - Streaming ZIP: zip.NewWriter(w) writes directly to http.ResponseWriter (no bytes.Buffer, no Content-Length)
    - Two-query pattern for GetBySlug: separate bucket row query then bucket_files query

key-files:
  created:
    - internal/domain/bucket/service.go
    - internal/storage/bucket_repo.go
  modified:
    - internal/domain/bucket/bucket.go

key-decisions:
  - "Bucket.DeleteSecret stored as plain text (from slug.New), compared with subtle.ConstantTimeCompare — same pattern as file service"
  - "URL scheme for buckets uses /b/ prefix (e.g. baseURL/b/slug, baseURL/b/delete/slug/secret) — distinct from file URLs"
  - "ListExpired fetches files per bucket via inner loop query — simpler than JOIN, consistent with GetBySlug two-query pattern"
  - "StorageKey per file generated by slug.New (not bucket slug) — each file gets its own independent on-disk key"

patterns-established:
  - "Bucket domain follows identical patterns to file domain (service, repo, errors, sentinel constants)"
  - "bucketRepo reuses nullableString() and boolToInt() helpers already declared in file_repo.go (same package)"

requirements-completed: [BUCK-01, BUCK-02, BUCK-03, BUCK-04, BUCK-05]

# Metrics
duration: 2min
completed: 2026-03-19
---

# Phase 03 Plan 02: Bucket Domain Service and SQLite Repository Summary

**BucketService with streaming ZIP, atomic one-use semantics, and bcrypt password enforcement backed by bucketRepo SQLite implementation**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-19T20:16:42Z
- **Completed:** 2026-03-19T20:19:15Z
- **Tasks:** 3
- **Files modified:** 3 (1 modified, 2 created)

## Accomplishments
- Extended Bucket entity with `DeleteSecret string` and `ExpiresAt *time.Time` fields; added five sentinel errors matching file domain pattern
- Implemented BucketService with Create (filestore-first with rollback), GetMeta, StreamZIP (direct to ResponseWriter via zip.NewWriter, no buffering), and Delete (subtle.ConstantTimeCompare)
- Implemented bucketRepo satisfying bucket.Repository with all 6 methods; compile-time assertion in place

## Task Commits

Each task was committed atomically:

1. **Task 1: Extend Bucket entity and update repository interface** - `560b9ff` (feat)
2. **Task 2: Implement BucketService** - `6d556b9` (feat)
3. **Task 3: Implement bucketRepo (SQLite)** - `4542d74` (feat)

## Files Created/Modified
- `internal/domain/bucket/bucket.go` - Added DeleteSecret/ExpiresAt fields, updated New() signature, added sentinel errors
- `internal/domain/bucket/service.go` - Full BucketService: Create, GetMeta, StreamZIP, Delete
- `internal/storage/bucket_repo.go` - SQLite bucketRepo implementing all 6 bucket.Repository methods

## Decisions Made
- URL scheme for buckets uses `/b/` prefix to distinguish from single-file URLs
- Each file within a bucket gets its own `storageKey` generated by `slug.New`, independent of the bucket slug
- `ListExpired` uses an inner-loop per-bucket file query for simplicity, consistent with `GetBySlug` two-query pattern

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- BucketService and bucketRepo are complete and compile cleanly; plan 03-04 (bucket handler) can wire these directly
- Expiry/cleanup worker (03-05) can use bucket.Repository.ListExpired as implemented
- No blockers for downstream plans

---
*Phase: 03-buckets-and-paste*
*Completed: 2026-03-19*
